import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.fb_login.app',
  appName: 'fb_login',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
